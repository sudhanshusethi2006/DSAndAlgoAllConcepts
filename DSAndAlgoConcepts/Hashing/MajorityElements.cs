namespace DSAndAlgoConcepts.Hashing
{
    internal class MajorityElements
    {
        public void FindFrequency()
        {

        }
        //static void Main(string[] args)
        //{
        //    int[] arr = new int[] { 1, 3, 2, 5, 1, 3, 1, 5, 1 };

        //    var map = arr.GroupBy(x => x).ToDictionary(x => x, x => x.Count());


        //    foreach (var item in map)
        //    {
        //        if (item.Value >= arr.Length / 3)
        //            Console.WriteLine(item.Key.Key);
        //    }

        //}
    }
}
